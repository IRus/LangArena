FROM langarena:base

RUN add-apt-repository ppa:ondrej/php -y

ARG version
RUN apt update \
    && apt install -y --no-install-recommends \
        php$version \
        php$version-cli \
        php$version-gmp \
    && apt clean
