FROM langarena:base_gcc

ARG version
RUN apt install -y --no-install-recommends gccgo-$version \
    && apt clean

RUN ln -sf /usr/bin/gccgo-$version /usr/bin/gccgo \
    && ln -sf /usr/bin/go-$version /usr/bin/go
    