FROM langarena:java

ARG version
RUN wget --progress=dot:giga -O - \
    https://github.com/jruby/jruby/releases/download/$version/jruby-bin-$version.tar.gz \
    | tar -xz
ENV PATH="/opt/jruby-$version/bin:${PATH}"
